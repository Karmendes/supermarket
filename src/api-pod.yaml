apiVersion: v1
kind: Pod
metadata:
  name: pod-flask
  labels:
    app: api-flask
spec:
  containers:
    - name: container-flask
      image: lucaseqt/api-flask:latest
      ports:
        - containerPort: 5000


